# راهنمای تداوم پروژه (Continuity Guide)

## چه چیزی باید از Kaggle نگه‌داشته/دانلود شود؟
✅ نگه دارید (کوچک، ضروری برای سابمیت نهایی):
- `data/processed/coco_format/*.json`
- `data/processed/yolo_detection/*/labels/*.txt` (فقط لیبل، بدون تصویر)
- چک‌پوینت‌های مدل (`outputs/checkpoints/*.pt`)
- `reports/` (نمودار، جدول، گزارش نهایی)
- خود کد (که در گیت‌هاب هم هست)

❌ لازم نیست نگه دارید:
- کپی/symlink تصاویر MRI (دیتاست عمومی کگل است؛ ارجاع به slug کافی است)

## چک‌لیست ضدِ قطع‌شدن
| ریسک | راه‌حل |
|---|---|
| Kaggle session تایم‌اوت می‌شود (سقف ۱۲ ساعت) | بعد از هر مرحله‌ی مهم (خصوصاً بعد از آموزش) → **Save Version → Quick Save** (نه Save & Run All، چون آن دوباره از صفر اجرا می‌کند) |
| چت کلود (رایگان) قطع/لیمیت می‌شود | `ROADMAP.md` + `configs/project_config.yaml` در گیت‌هاب = منبع واحد حقیقت. در چت جدید، لینک ریپو را بدهید و بگویید «ادامه‌ی پروژه» |
| فراموش‌کردن push کردن آخرین تغییرات | بعد از دریافت هر فایل از من → فوراً commit + push، حتی اگر فاز کامل نشده |

## نام‌گذاری نسخه‌های Kaggle (پیشنهادی)
`phase2-data-converted`, `phase3-fastercnn-trained`, `phase4-yolo11-trained`, ...

## Phase 3 — چه چیزی از کگل بردارید؟
- نتیجه‌ی نهایی (با augmentation): `outputs_v3/`
- اجرای کنترل/ablation (بدون augmentation): `outputs_ablation_baseline/`

بعد از اجرای `train_faster_rcnn.py` (چند ساعت طول می‌کشد، حتماً قبل از شروع Quick Save بزنید تا اگر
session قطع شد چیزی از دست نرود، و در حین آموزش هم هر چند ساعت Quick Save بزنید):

✅ **بردارید و در گیت‌هاب/جای امن دیگر نگه دارید:**
- `outputs_v3/logs/faster_rcnn_train_log.txt` (کوچک، حتماً commit کنید — برای نمودار loss در گزارش لازم است)
- `outputs_v3/checkpoints/faster_rcnn_last.pt` (~165MB — **در گیت‌هاب کامیت نکنید**، فقط دانلود کنید و جایی
  نگه دارید؛ چون از سقف معمول حجم فایل گیت‌هاب بزرگ‌تر/نزدیک است. اگر لازم شد در گزارش نهایی به آن ارجاع دهید)

❌ چک‌پوینت‌های میانی (`faster_rcnn_epoch{5,10,...}.pt`) لازم نیست نگه دارید مگر بخواهید منحنی یادگیری
   را روی چک‌پوینت‌های مختلف هم بررسی کنید.

## ⚠️ آموزش طولانی (Phase 3/4) — هرگز Interactive اجرا نکنید
اجرای تعاملی (کلیک روی Run هر سلول) روی Kaggle برای کارهای بیش از ۳۰ دقیقه رسماً توصیه نمی‌شود؛
idle timeout یک‌ساعته دارد و با قطع اینترنت/بستن تب از بین می‌رود.

**روش درست:** `Save Version → Save & Run All (Commit)` — کل نوت‌بوک را روی سرور خود Kaggle،
مستقل از اینترنت/مرورگر شما، اجرا می‌کند. می‌توانید ببندید و بعداً از «Version History» نتیجه
و لاگ‌ها را ببینید/دانلود کنید.

**اگر با این حال قطع شد (مثلاً quota هفتگی GPU تمام شد):** `train_faster_rcnn.py` حالا خودکار
resume دارد — یک فایل `outputs_v3/checkpoints/training_state.pt` بعد از هر epoch ذخیره می‌شود
(شامل epoch، وزن مدل، optimizer، scheduler). کافی است دوباره همان دستور را Commit کنید؛
اسکریپت خودش تشخیص می‌دهد و دقیقاً از epoch بعدی ادامه می‌دهد، نه از صفر.
این تست شد: در یک شبیه‌سازی، اجرای اول تا epoch 3 رفت، اجرای دوم دقیقاً از epoch 4 ادامه داد.
